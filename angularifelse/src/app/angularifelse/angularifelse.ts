import { Component } from '@angular/core';

@Component({
  selector: 'app-angularifelse',
  imports: [],
  templateUrl: './angularifelse.html',
  styleUrl: './angularifelse.css'
})
export class Angularifelse {
         serverRunning=false
}
